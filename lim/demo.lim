# This function adds two integers.
fn add(n1, n2)
  sum = n1 + n2
  return sum
end

# This function multiplies two integers.
fn multiply(n1, n2)
  # product = n1 * n2
  # return product
  return n1 * n2
end



v = add(2, 3) + multiply(3, 4)
print v

